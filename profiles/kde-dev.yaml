metadata:
  name: kde-dev
  description: Fedora KDE Plasma Workspaces for developers profile
  version: "1.0.0"
  maintainer: ephmo
  release_date: "2026-01-02"
  last_modified: "2026-01-02"

requires:
  distro: fedora
  distro_versions:
    - "42"
    - "43"
  arch: x86_64

repos:
  vscode: true

packages:
  install:
    all_versions:
      - "@admin-tools"
      - "@base-graphical"
      - "@container-management"
      - "@desktop-accessibility"
      - "@dial-up"
      - "@firefox"
      - "@fonts"
      - "@guest-desktop-agents"
      - "@hardware-support"
      - "@input-methods"
      - "@kde-desktop"
      - "@libreoffice"
      - "@multimedia"
      - "@networkmanager-submodules"
      - "@printing"
      - "@standard"
      - code
      - git
      - neovim
      - shfmt

services:
  set-default:
    - graphical.target

  enable:
    - sddm.service
